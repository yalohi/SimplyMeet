services:
  # ======================================================================================
  # Regular services will download a pre-built version from the GitHub container registry.
  # ======================================================================================

  wasm:
    hostname: wasm
    container_name: simplymeet-wasm
    image: ghcr.io/yalohi/simplymeet-wasm:latest
    ports:
      - 5001:5001/tcp
    volumes:
      - ./wasm/config/appsettings.Production.json:/etc/nginx/html/appsettings.Production.json:ro

  api:
    hostname: api
    container_name: simplymeet-api
    image: ghcr.io/yalohi/simplymeet-api:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - JWT_SECRET="PUT_A_STRONG_SECRET_HERE" # 32+ Characters
    ports:
      - 5002:5002/tcp
    volumes:
      - ./api/config/appsettings.Production.json:/app/appsettings.Production.json:ro
      - ./api/data/:/app/App_Data

  # ===============================================================================================
  # *-source-github services will clone the remote repository from GitHub and then build from that.
  # ===============================================================================================

  # wasm-source-github:
  #   hostname: wasm
  #   container_name: simplymeet-wasm
  #   build:
  #     dockerfile: dockerfile-wasm-github
  #   image: simplymeet-wasm
  #   ports:
  #     - 5001:5001/tcp
  #   volumes:
  #     - ./wasm/config/appsettings.Production.json:/etc/nginx/html/appsettings.Production.json:ro

  # api-source-github:
  #   hostname: api
  #   container_name: simplymeet-api
  #   build:
  #     dockerfile: dockerfile-api-github
  #   image: simplymeet-api
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Production
  #     - JWT_SECRET="PUT_A_STRONG_SECRET_HERE" # 32+ Characters
  #   ports:
  #     - 5002:5002/tcp
  #   volumes:
  #     - ./api/config/appsettings.Production.json:/app/appsettings.Production.json:ro
  #     - ./api/data/:/app/App_Data

  # =====================================================================================================================================
  # *-source-local services will build from source files already present on your machine. It will not download them from a remote server.
  # =====================================================================================================================================

  # wasm-source-local:
  #   hostname: wasm
  #   container_name: simplymeet-wasm
  #   build:
  #     context: ..
  #     dockerfile: Docker/dockerfile-wasm-local
  #   image: simplymeet-wasm
  #   ports:
  #     - 5001:5001/tcp
  #   volumes:
  #     - ./wasm/config/appsettings.Production.json:/etc/nginx/html/appsettings.Production.json:ro

  # api-source-local:
  #   hostname: api
  #   container_name: simplymeet-api
  #   build:
  #     context: ..
  #     dockerfile: Docker/dockerfile-api-local
  #   image: simplymeet-api
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Production
  #     - JWT_SECRET="PUT_A_STRONG_SECRET_HERE" # 32+ Characters
  #   ports:
  #     - 5002:5002/tcp
  #   volumes:
  #     - ./api/config/appsettings.Production.json:/app/appsettings.Production.json:ro
  #     - ./api/data/:/app/App_Data
